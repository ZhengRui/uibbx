import { atom } from "jotai";

export const authPanelOpenAtom = atom(false);
export const authModeAtom = atom("login");
export const downloadPanelOpenAtom = atom(false);

export const accountTabAtom = atom("likes");
